// When backend is deployed to Vercel as a serverless function the Express app
// is mounted under `/api`. Point frontend to the API root.
export const BASE_API = "https://sweetbytes-five.vercel.app/api";
